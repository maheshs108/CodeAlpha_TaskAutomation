This project automates three real-world tasks using Python:

Move .jpg files from one folder to another

Extract email addresses from a text file

Scrape the title of a webpage and save it

A menu-based interface allows the user to choose any task.

🛠 Technologies Used

Python 3

os, shutil, re

requests (external library)